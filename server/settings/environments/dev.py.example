from server.settings.components.celery import CELERY_BROKER_URL

SITE_ID = 1

DEBUG = True

LANGUAGE_CODE = 'en'

SECRET_KEY = '*'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': '',
        'USER': 'postgres',
        'PASSWORD': 'postgres',
        'HOST': 'localhost'
    }
}

REDIS_CONNECTION_STRING = CELERY_BROKER_URL

SOCIAL_AUTH_GITLAB_KEY = '*'
SOCIAL_AUTH_GITLAB_SECRET = '*'
