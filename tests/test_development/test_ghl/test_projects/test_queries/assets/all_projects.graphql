query ($state: [ProjectState], $offset: Int, $first: Int) {
  allProjects(state: $state, offset: $offset, first: $first) {
    count
    edges {
      node {
        id
        title
      }
    }
  }
}
