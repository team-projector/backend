query ($id: ID!) {
  team(id: $id) {
    members(orderBy: [USER_NAME_ASC]) {
      count
      edges {
        node {
          roles
          user {
            id
            name
          }
        }
      }
    }
  }
}
